# Local Supabase credentials — never commit
js/supabase-config.js

# Temporary QA flags — never commit (see js/dev-flags.example.js)
js/dev-flags.js

# Node (local dev server)
node_modules/

# Environment files
.env
.env.local
.env.*.local
.env.rls-test
.env.rls-test.local
.env.test-export
.env.test-export.local

# Local raw real-hotel handover exports (may contain guest PII) — never commit.
# Fictional Pilot Hotel CSVs under testing/pilot-hotel/exports/ may still be committed.
# See testing/EXPORT_TEST_HANDOVERS.md
testing/**/exports/local-raw/

# Dependencies
node_modules/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Supabase CLI local state (linked project ref, cached versions) — not deployment config
supabase/.temp/

# Build output
dist/
build/
